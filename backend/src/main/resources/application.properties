spring.application.name=store

# Supabase Database Configuration
# IMPORTANT: Set these environment variables with your actual Supabase credentials
# DO NOT commit real credentials to version control
spring.datasource.url=jdbc:postgresql://aws-1-ca-central-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.jrmgouykfgubpiqrynbh
spring.datasource.password=hW6YOa2tJ9Xp7xtt
spring.datasource.driver-class-name=org.postgresql.Driver

# H2 In-Memory Database Configuration (for testing only - currently disabled)
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.username=sa
# spring.datasource.password=
# spring.datasource.driver-class-name=org.h2.Driver
# spring.h2.console.enabled=true

# JPA Configuration for Supabase
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Connection Pool Settings for Supabase
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Server Configuration
server.port=${PORT:8080}

# Logging
logging.level.com.SecondSavour.store=INFO
logging.level.org.springframework.security=INFO

# Security Configuration
spring.security.require-ssl=false
