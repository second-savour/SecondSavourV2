import { Metadata } from "next";

export const metadata = {
  title: "Sign Up - Second Savour",
  description: "Create your Second Savour account to start shopping for sustainable citrus treats.",
};

export default function SignUpLayout({ children }) {
  return children;
}

